# Copy this file to ".env" and fill in real values.
# Never commit the real .env file to git.

# "mock" (default, no key needed), "openai", or "gemini"
LLM_PROVIDER=mock

OPENAI_API_KEY=sk-...
GEMINI_API_KEY=...
